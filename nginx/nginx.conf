events {}

http {
    server {
        listen 8080;

        location / {
            proxy_pass http://api:8080;

            # Headers requis
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
